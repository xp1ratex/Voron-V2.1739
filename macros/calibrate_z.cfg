[gcode_macro CALIBRATE_Z]
rename_existing: _BASE_CALIBRATE_Z
gcode:
    {% set mesh_ready = printer["gcode_macro _ADAPTIVE_MESH_VARIABLES"].ready %}
    {% set mesh_center = printer["gcode_macro _ADAPTIVE_MESH_VARIABLES"].mesh_center %}
    status_calibrating_z
    CG28
    M117 Z-Calibration..
    ATTACH_PROBE                # a macro for fetching the probe first
    {% if mesh_ready %}
        RESPOND MSG="Z calibration: a mesh is computed and ready, probing mesh center: {mesh_center}"
        _BASE_CALIBRATE_Z BED_POSITION={mesh_center}
    {% else %}
        RESPOND MSG="Z calibration: no mesh computed, probing default point"
        _BASE_CALIBRATE_Z
    {% endif %}
    DOCK_PROBE                # and parking it afterwards
    M117

[z_calibration]
probe_nozzle_x: 206
probe_nozzle_y: 305
probe_switch_x: 213
probe_switch_y: 283.5
probe_bed_x: 150
probe_bed_y: 150 
switch_offset: 0.350 # Decreasing will move the nozzle farther away from the  was 0.561 211011 was 0.520 220321, 0.530 220402, 0.550 220714 (0.4 nozzle) was 0.0495 before pc-cf 220806 0.400 fortfarande låg 220807
max_deviation: 2 #was 1
speed: 300
clearance: 10 
probing_second_speed: 5
probing_retract_dist: 2
probing_first_fast: true